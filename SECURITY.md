# Security Policy

This is a sandbox repository and should never contain secrets or production data.

If you notice a leaked credential or sensitive artifact, open a minimal issue without repeating the secret, or contact `v0idch3cksum` on Discord.
